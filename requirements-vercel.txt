# Vercel-Optimized Dependencies
# Размер: ~80-100 MB (вместо 437 MB original)

# Core Telegram Bot (легкие, ~5 MB)
pyTelegramBotAPI==4.14.0
python-dotenv>=1.0.1
requests==2.31.0

# AI Services (средние, ~15 MB)
openai>=1.91.0
anthropic==0.57.0
zep-cloud==2.14.1

# Knowledge Base (ОПТИМИЗИРОВАНО)
qdrant-client>=1.7.0
# ❌ sentence-transformers УДАЛЕНО (~900 MB с PyTorch+CUDA)
# ✅ fastembed добавлен ниже (~30 MB, если нужен)
# Примечание: Если embeddings генерируются только в migration scripts,
# можно НЕ включать fastembed в runtime requirements

# Vector Embeddings (опционально, только если используется в runtime)
# Раскомментировать если бот генерирует embeddings в runtime:
# fastembed>=0.3.0

# Web Framework (легкие, ~10 MB)
fastapi==0.110.0
uvicorn[standard]==0.27.0
pydantic>=2.11.5

# Database - PostgreSQL (вместо MySQL)
SQLAlchemy==2.0.25
psycopg2-binary==2.9.9  # ✅ PostgreSQL driver (вместо pymysql)
cryptography==42.0.5

# Voice Processing (средние, ~8 MB)
aiohttp==3.10.11
aiofiles==24.1.0
httpx==0.27.2

# ❌ УДАЛЕНЫ тяжелые/ненужные зависимости:
# - sentence-transformers (~900 MB) → fastembed (~30 MB) если нужен
# - graphiti-core (только в scripts, не в runtime)
# - supabase (не используется, есть Qdrant)
# - streamlit (админ панель, не нужна в production)
# - alembic (миграции БД выполняются отдельно)
# - pymysql (заменён на psycopg2-binary для PostgreSQL)
