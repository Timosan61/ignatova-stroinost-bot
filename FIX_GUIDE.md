# 🔧 Гид по исправлению неполных ответов бота

**Дата:** 13 ноября 2025
**Проблема:** Бот отвечал только "Поняла ваш вопрос! Подготовлю детальный ответ специально для вас. Минуточку!" вместо полных ответов

---

## 🎯 Что было исправлено

### 1. **Улучшена обработка ошибок LLM** (`bot/agent.py`)

**Проблема:**
- При ошибке OpenAI/Anthropic API бот возвращал статичный fallback текст
- Информация из базы знаний игнорировалась при ошибках
- Не было детального логирования для диагностики

**Решение:**
```python
# Теперь при ошибке LLM:
1. Проверяется наличие информации в базе знаний
2. Если есть - возвращается найденная информация с источниками
3. Добавлено детальное логирование всех этапов
4. Более информативные fallback сообщения
```

**Изменения в коде:**
- ✅ Добавлено логирование на каждом этапе генерации ответа
- ✅ Автоматическое добавление источников в ответы (`📚 **Источник:** SCRIPTS-сессия1`)
- ✅ Улучшенный fallback с использованием базы знаний
- ✅ Детализированные сообщения об ошибках

### 2. **Исправлена точка входа в Dockerfile**

**Проблема:**
```dockerfile
CMD python -m uvicorn webhook:app ...  # Старая точка входа
```

**Решение:**
```dockerfile
CMD python -m uvicorn main:app ...     # Новая точка входа (v2.0)
```

### 3. **Добавлены тестовые скрипты**

Новые файлы для диагностики:
- `test_bot_webhook.py` - тест webhook endpoint
- `test_railway_env.py` - проверка переменных окружения
- `test_voice_enabled.py` - тест голосовых функций
- `test_voice_service.py` - тест голосового сервиса

---

## 📊 Возможные причины проблемы

### **1. Ошибка API ключей (наиболее вероятно)**

**Симптомы:**
- Бот отвечает только заглушками
- В логах: `❌ Ошибка OpenAI: ...` или `❌ Ошибка Anthropic: ...`

**Решение:**
```bash
# Проверить на Railway:
1. Зайти в Railway Dashboard
2. Проверить переменные окружения:
   - OPENAI_API_KEY - должен начинаться с "sk-"
   - ANTHROPIC_API_KEY - должен начинаться с "sk-ant-"
   - ZEP_API_KEY - должен начинаться с "z_"
3. Проверить баланс на платформах OpenAI/Anthropic
```

### **2. Rate Limit исчерпан**

**Симптомы:**
- Бот работал, потом перестал отвечать полноценно
- Ошибки типа "429 Too Many Requests"

**Решение:**
- Подождать (rate limit обычно сбрасывается через 1 минуту)
- Проверить лимиты на OpenAI Dashboard
- Рассмотреть upgrade тарифного плана

### **3. Баланс API истек**

**Симптомы:**
- Ошибки аутентификации
- "Insufficient quota"

**Решение:**
- Пополнить баланс OpenAI
- Проверить срок действия API ключей

---

## 🔍 Как проверить что исправление сработало

### **Шаг 1: Дождаться деплоя Railway**

```bash
# Railway автоматически задеплоит обновление из GitHub
# Обычно занимает 2-5 минут

# Проверить статус:
1. Зайти на https://railway.app
2. Открыть проект ignatova-stroinost-bot
3. Проверить что деплой завершился успешно (зеленый статус)
```

### **Шаг 2: Проверить логи Railway**

```bash
# В Railway Dashboard:
1. Deployments -> Latest deployment
2. View Logs
3. Искать:
   - ✅ "OpenAI клиент инициализирован"
   - ✅ "Anthropic клиент инициализирован"
   - ✅ "Zep клиент инициализирован"
   - ❌ "Ошибка OpenAI: ..." (если есть - проблема с API ключом)
```

### **Шаг 3: Протестировать бота**

**Отправить боту тестовое сообщение:**
```
Как обработать возражение "нет времени"?
```

**Ожидаемый результат (ХОРОШИЙ):**
```
Понимаю. Давайте я закреплю за вами слот на вечер — после 18:00
или 19:00, что удобнее? Займёт всего 10-15 минут.

[детальный ответ по скриптам...]

📚 **Источник:** OBJECTIONS-сессия1
```

**Плохой результат (если проблема осталась):**
```
Поняла ваш вопрос! Подготовлю детальный ответ специально для вас.
Минуточку!

Кристина, ignatova-stroinost
```

### **Шаг 4: Проверить логи бота**

**В Railway логах искать:**
```
🤖 Генерируем ответ для: 'Как обработать возражение...'
📊 Найдено источников в базе знаний: 2
✅ OpenAI ответ получен
✅ Ответ сгенерирован успешно (длина: 450 символов)
```

**Если видите ошибки:**
```
❌ КРИТИЧЕСКАЯ ОШИБКА LLM: ...
```
→ Проблема с API ключами, см. раздел "Устранение проблем"

---

## 🛠️ Устранение проблем

### **Проблема: "❌ Ошибка OpenAI: Incorrect API key"**

**Решение:**
1. Проверить OPENAI_API_KEY на Railway
2. Убедиться что ключ начинается с `sk-`
3. Проверить что ключ не истек на platform.openai.com
4. Создать новый ключ если нужно
5. Обновить переменную на Railway
6. Перезапустить деплой

### **Проблема: "❌ Ошибка OpenAI: You exceeded your current quota"**

**Решение:**
1. Пополнить баланс на platform.openai.com
2. Проверить Usage на OpenAI Dashboard
3. Рассмотреть upgrade на платный тариф

### **Проблема: Fallback срабатывает, но показывает базу знаний**

**Это нормально!** Означает что:
- ✅ База знаний Zep работает
- ❌ OpenAI/Anthropic API недоступны
- ℹ️ Бот использует улучшенный fallback

**Ответ будет выглядеть так:**
```
⚠️ AI временно недоступен, но нашла информацию в базе знаний:

[релевантная информация из Zep]

📚 **Источник:** SCRIPTS-сессия2

🔄 Попробуйте задать вопрос еще раз или уточните запрос.

Кристина, ignatova-stroinost
```

---

## 📈 Ожидаемые улучшения

После исправления:

1. **✅ Полные ответы вместо заглушек**
   - Детальные скрипты продаж
   - Обработка возражений
   - Конкретные рекомендации

2. **✅ Автоматическое указание источников**
   ```
   📚 **Источник:** OBJECTIONS-сессия2, SCRIPTS-сессия1
   ```

3. **✅ Улучшенная диагностика**
   - Детальные логи для отладки
   - Понятные сообщения об ошибках
   - Прозрачность работы AI

4. **✅ Graceful degradation**
   - Если AI недоступен → показываем базу знаний
   - Если база знаний пуста → понятное сообщение
   - Никогда не просто "Минуточку!"

---

## 🔄 Что делать если проблема осталась

### 1. Собрать диагностическую информацию

**Проверить endpoint диагностики:**
```bash
curl https://ignatova-stroinost-bot-production.up.railway.app/debug/env
```

**Должно показать:**
```json
{
  "env_vars": {
    "TELEGRAM_BOT_TOKEN": "✅ Set",
    "OPENAI_API_KEY": "✅ Set",  // ← Важно!
    "ANTHROPIC_API_KEY": "✅ Set",
    "ZEP_API_KEY": "✅ Set"
  },
  "ai_enabled": true,
  "agent_initialized": true
}
```

### 2. Проверить логи Railway

```bash
# В Railway Dashboard:
Deployments → Latest → View Logs

# Искать:
- "✅ OpenAI клиент инициализирован"
- "✅ Zep клиент инициализирован"
- Любые ошибки с "❌"
```

### 3. Проверить API ключи

**OpenAI:**
- https://platform.openai.com/api-keys
- Проверить срок действия
- Проверить баланс в Billing

**Anthropic:**
- https://console.anthropic.com/settings/keys
- Проверить срок действия
- Проверить лимиты

**Zep:**
- https://app.getzep.com
- Проверить API key в Settings

### 4. Если всё равно не работает

**Создать новые API ключи:**
```bash
1. OpenAI: platform.openai.com/api-keys → "Create new secret key"
2. Anthropic: console.anthropic.com/settings/keys → "Create Key"
3. Обновить на Railway:
   - Settings → Variables
   - OPENAI_API_KEY = новый ключ
   - ANTHROPIC_API_KEY = новый ключ
4. Restart deployment
```

---

## 📞 Техподдержка

Если проблема не решается:

1. **Скопировать логи ошибок** из Railway
2. **Сделать скриншот** конфигурации переменных окружения (скрыв значения)
3. **Показать пример** диалога с ботом где видна проблема
4. **Отправить всё это** администратору системы

---

## ✅ Чеклист проверки

После обновления убедитесь:

- [ ] Railway деплой завершился успешно (зеленый статус)
- [ ] В логах видно "✅ OpenAI клиент инициализирован"
- [ ] В логах видно "✅ Zep клиент инициализирован"
- [ ] Бот отвечает полными ответами (не заглушками)
- [ ] В ответах есть указание источников `📚 **Источник:**`
- [ ] Нет ошибок типа "❌ Ошибка OpenAI" или "❌ Ошибка Anthropic"

---

**Обновление:** 13 ноября 2025
**Коммит:** c810299
**Статус:** ✅ Отправлено на GitHub, Railway автоматически деплоит
